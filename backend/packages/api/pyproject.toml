[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dmitkov28", email = "mitkov.dimitar@yahoo.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.3",
]

[project.scripts]
api = "api:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
